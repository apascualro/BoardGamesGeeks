
//localhost
const NODE_ENV = "localhost";
const SERVER_PORT = "8080";

const config = {
  env: NODE_ENV,
  port: SERVER_PORT,
};

//heroku
// const NODE_ENV = "https://heroku-angular-25.herokuapp.com/";
// const SERVER_PORT = "";

// const config = {
//   env: NODE_ENV,
//   port: SERVER_PORT,
// };
 module.exports = config;